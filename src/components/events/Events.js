import React from 'react';
import styles from "./Events.module.scss"

const Events = ({ name, age }) => {


  return (
    <div>
     <h1 className={styles.colorRed}>Events</h1>
    </div>
  );
};

export default Events;
